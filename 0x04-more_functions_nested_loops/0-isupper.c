#include "main.h"

/**
 * _isupper - checks character is uppercase
 * @c: variable text
 * Return: )
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
