#include "main.h"
/**
 * _ads - compute absolut value to integer
 * @t: the integer to be compute
 * Return: the absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
